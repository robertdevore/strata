#!/usr/bin/env node

// Retired: use the authenticated, bounded CLI/API contract.
console.error(
  'LEGACY_INTERFACE_RETIRED: Use node scripts/strata.mjs or the installed strata CLI. See docs/legacy-interfaces.md.',
)
process.exitCode = 1
